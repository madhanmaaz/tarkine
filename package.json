{
  "name": "tarkine",
  "version": "0.0.6",
  "description": "tarkine - template engine.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "browser": "dist/browser.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/madhanmaaz/tarkine"
  },
  "author": {
    "name": "madhanmaaz",
    "url": "http://madhanmaaz.netlify.app/"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build:node": "esbuild src/index.js --outfile=dist/index.js --platform=node --format=cjs --bundle --minify && esbuild src/index.js --outfile=dist/index.mjs --platform=node --format=esm --bundle --minify",
    "build:browser": "esbuild src/browser.js --outfile=dist/browser.js --platform=browser --global-name=tarkine --bundle --minify",
    "build": "npm run build:node && npm run build:browser"
  },
  "keywords": [
    "template",
    "engine",
    "html",
    "rendering",
    "express",
    "tarkine",
    "tarkine template",
    "tarkine template engine"
  ],
  "license": "MIT"
}